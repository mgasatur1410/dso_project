# Trivy Ignore файл (P07 C4 2**)
# Список игнорируемых CVE с обоснованием

# Пример игнорирования уязвимости (раскомментируйте при необходимости):
# CVE-2024-XXXXX - Уязвимость в dev-зависимости, не используется в продакшене
# CVE-2024-YYYYY - Ложное срабатывание для Python 3.11-slim базового образа

# Реальные CVE добавляйте сюда с комментариями-обоснованиями
# Формат: CVE-YYYY-NNNNN
